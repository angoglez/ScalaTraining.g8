package m1.e2

import org.scalatest.funsuite.AnyFunSuite
import org.scalatest.matchers.should.Matchers

// This time, we will provide you a list of test
// covering the minimum cases.

// TODO: write them all and add other tests that you think might be interesting


class BinaryTreeTest extends AnyFunSuite with Matchers {

  test("BT stores one element") {
    true shouldBe true
  }

  test("BT inserts in left an element smaller than the root") {
    true shouldBe true
  }

  test("BT inserts in right an element greater than the root") {
    true shouldBe true
  }

  test("BT doesn't change when inserting one element already stored"){
    true shouldBe true
  }

  test("BT inserts in right correctly with some levels of depth"){
    true shouldBe true
  }

  test("BT inserts in left correctly with some levels of depth"){
    true shouldBe true
  }

  test("BT searches for a stored element correctly"){
    true shouldBe true
  }

  test("BT manages search for an unstored element"){
    true shouldBe true
  }
}
